@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model AuthorizationVkontakte.Controllers.LoginViewModel

<h3>Login with external provider</h3>

<form asp-controller="Admin" asp-action="ExternalLogin" asp-route-returnUrl="@Model.ReturnUrl">
    @foreach (var provider in @Model.ExternalProviders)
    {
        <button type="submit" name="@provider" value="@provider.Name">@provider.DisplayName</button>
    }
</form>

<form asp-controller="Admin" asp-action="Login" method="post">
    <input hidden asp-for="ReturnUrl"/>
    <div>
        <label>Username</label>
        <input asp-for="Username" />

        <label>Password</label>
        <input asp-for="Password" />
    </div>
    
    <button type="submit">Войти</button>
</form>